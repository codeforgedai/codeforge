import type { RealtimeProvider } from './provider.js';

export function setupWebSocket(_server: any, _realtime: RealtimeProvider) {
  // WebSocket upgrade handler stub
  // Will use ws or the built-in WebSocket from ultimate-express
}
