import { describe, it } from 'vitest';

describe('goals route', () => {
  it('creates a goal');
  it('lists goals by orgId');
  it('gets goal by id');
  it('updates goal fields');
  it('lists child goals');
});
