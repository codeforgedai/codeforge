import { describe, it } from 'vitest';

describe('tasks route', () => {
  it('creates a task');
  it('lists tasks by orgId');
  it('gets task by id');
  it('updates task fields');
  it('creates a comment');
  it('parses @mentions in comments');
});
