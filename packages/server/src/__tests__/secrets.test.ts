import { describe, it } from 'vitest';

describe('secrets route', () => {
  it('creates a secret with encrypted value');
  it('lists secrets by orgId');
  it('creates a new version');
  it('decrypts a secret version');
});
