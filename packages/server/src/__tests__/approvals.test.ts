import { describe, it } from 'vitest';

describe('approvals route', () => {
  it('creates an approval');
  it('lists approvals by orgId');
  it('decides an approval');
  it('rejects deciding already-decided approval');
});
