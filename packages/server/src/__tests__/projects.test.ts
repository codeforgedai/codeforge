import { describe, it } from 'vitest';

describe('projects route', () => {
  it('creates a project');
  it('lists projects by orgId');
  it('gets project by id');
  it('adds a workspace to a project');
  it('adds a doc to a project');
});
